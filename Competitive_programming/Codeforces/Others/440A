    #include <bits/stdc++.h>
    using namespace std;
     
    int main() {
    	int n, c = 1; cin >> n;
    	for (int i = 1, a; i < n; i++) {
    		cin >> a;
    		c ^= (i + 1) ^ a;
    	}
    	cout << c << '\n';
    }
